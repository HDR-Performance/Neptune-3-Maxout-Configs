# Configuration archive

These folders are for inactive backups only. Klipper will not include anything
from them unless an active configuration explicitly references it.

- `printer-backups/`: old printer configurations
- `moonraker-backups/`: old Moonraker configurations
- `config-exports/`: downloaded configuration ZIP archives

Keep active and updater-managed files, plus `KAMP/`, in the `/config` root.

